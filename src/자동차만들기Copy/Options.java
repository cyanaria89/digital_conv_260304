package 자동차만들기Copy;

public class Options {
}

interface AirCon {
    String airCon(boolean onOff);
}

interface Audio {
    String audio(boolean onOff);
}

interface AutoPilot {
    String autoPilot(boolean onOff);
}
