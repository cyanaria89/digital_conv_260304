package OOP_자동차만들기Answer;

public class Options {
    // 그냥 파일명이 맘에 안들어서 만들었어요~
}

interface Audio {
    void audioOnOff(boolean isOn);
}

interface AutoDrive {
    void autoOnOff(boolean isOn);
}

interface AirCon {
    void airConOnOff(boolean isOn);
}